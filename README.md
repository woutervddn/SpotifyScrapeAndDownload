SpotifyScrapeAndDownload
========================

this repo contains a perl script that converts a spotify uri to a playlist of tracks and artists; a shell scripts that gives the most likely youtube url for a given query and a shell script that combines the two and downloads all songs from a spotify uri via youtube